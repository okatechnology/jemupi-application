import React from 'react';
import styled from 'styled-components';

const Title = () => {
  return <CustomTitle>SHARING</CustomTitle>;
};

const CustomTitle = styled.h1`
  font-size: 4rem;
  margin: 0;
`;

export default Title;
