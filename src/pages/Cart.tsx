import React from 'react';
import Header from '../components/Header/Header';

interface CartProps {}

const Cart = ({}: CartProps) => {
  return (
    <>
      <Header />
    </>
  );
};

export default Cart;
